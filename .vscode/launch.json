{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Web Server",
            "type": "node",
            "request": "launch",

            "runtimeArgs": [
                "-r",
                "source-map-support/register",
                "-r",
                "dotenv/config",
            ],

            "envFile": "${workspaceRoot}/.env",
            "program": "${workspaceRoot}/dist/web/www.js",
        },
    ],
}
